package com.ownwn.feature;

import net.minecraft.client.gui.DrawContext;
import net.minecraft.scoreboard.ScoreboardObjective;

public class SidebarChanger {
    public static void stuff(DrawContext context, ScoreboardObjective objective) {
        // TODO: this

//            System.out.println(Objects.requireNonNull(a.getPlayerName()));
//            objective.getScoreboard().getTeam("a").
//            MutableText text = Team.decorateName(objective.getScoreboard().getPlayerTeam(a.getPlayerName()), Text.literal(a.getPlayerName()));
//            System.out.println(text);
//            if (MinecraftClient.getInstance().player != null) {
//                Chat.chat(text);
//            }


//        for (ScoreboardObjective b : objective.getScoreboard().getObjectives()) {
//            System.out.println(b.getDisplayName());
//        }
    }
}
